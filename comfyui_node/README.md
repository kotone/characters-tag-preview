# ComfyUI 角色标签选择器节点

## 功能简介

这是一个 ComfyUI 自定义节点，用于从游戏角色数据库中选择角色并生成各种格式的标签输出。

支持的游戏：
- 原神 (Genshin Impact)
- 崩坏：星穹铁道 (Honkai: Star Rail)
- 绝区零 (Zenless Zone Zero)
- 鸣潮 (Wuthering Waves)

## 功能特性

- 📚 **自动加载数据**: 从 JSON 文件自动加载所有角色数据
- 🎮 **作品筛选**: 根据选择的游戏/作品筛选角色列表
- 🎨 **角色选择**: 下拉选择具体角色（显示中英文名）
- 📤 **多种输出格式**:
  - **标签格式**: `the_dahlia_(honkai_impact)` - 适用于 Stable Diffusion/SDXL
  - **自然语言**: `大丽花, The Dahlia: 崩坏：星穹铁道` - 更易读的格式

## 安装方法

### 方式一：手动安装（推荐）

1. 将整个 `comfyui_node` 文件夹复制到 ComfyUI 的自定义节点目录：
   ```
   ComfyUI/custom_nodes/character-tag-selector/
   ```

2. 创建数据符号链接或复制数据文件：
   - 在 `character-tag-selector/` 下创建 `output` 文件夹
   - 将所有 `*_characters-en-cn.json` 文件复制到此目录

3. 你的目录结构应该是：
   ```
   ComfyUI/
   └── custom_nodes/
       └── character-tag-selector/
           ├── __init__.py
           ├── character_tag_selector.py
           ├── README.md
           └── output/
               ├── genshin_characters-en-cn.json
               ├── honkai_starrail_characters-en-cn.json
               ├── zzz_characters-en-cn.json
               └── wuthering_waves_characters-en-cn.json
   ```

4. 重启 ComfyUI

### 方式二：使用符号链接

如果你想直接使用项目中的数据文件而不复制：

**Windows (需要管理员权限):**
```powershell
cd ComfyUI\custom_nodes\character-tag-selector
mklink /D output "E:\person\characters-tag-preview\output"
```

**Linux/Mac:**
```bash
cd ComfyUI/custom_nodes/character-tag-selector
ln -s /path/to/characters-tag-preview/output output
```

## 使用方法

1. 在 ComfyUI 中，右键添加节点
2. 找到 `🎮 Character Tags` 分类
3. 选择 `🎮 Character Tag Selector` 节点
4. 配置参数：
   - **source (作品)**: 选择游戏/作品
   - **character (角色)**: 选择具体角色
   - **output_type (输出类型)**: 选择输出格式
5. 连接到其他节点（如文本框、提示词节点等）

## 输出示例

### 标签格式
```
the_dahlia_(honkai_impact)
chisa_(wuthering_waves)
ellen_joe_(zenless_zone_zero)
```

### 自然语言格式
```
大丽花, The Dahlia: 崩坏：星穹铁道
千咲, Chisa: 鸣潮
艾莲, Ellen Joe: 绝区零
```

## 节点参数说明

| 参数 | 类型 | 说明 |
|------|------|------|
| source | 下拉选择 | 选择作品后，角色列表会自动筛选该作品的角色 |
| character | 下拉选择 | 显示格式：中文名 (英文名) |
| output_type | 下拉选择 | 选择输出标签的格式 |

## 输出说明

- **输出类型**: STRING (文本)
- **输出名称**: text
- **用途**: 可连接到任何接受文本输入的节点

## 注意事项

1. **数据文件**: 确保 JSON 数据文件存在且格式正确
2. **编码**: 所有文件使用 UTF-8 编码
3. **更新数据**: 更新 JSON 文件后需要重启 ComfyUI 或重新加载节点

## 数据格式

JSON 数据文件应包含以下字段：

```json
[
  {
    "name_cn": "大丽花",
    "name_en": "The Dahlia",
    "tag": "the_dahlia_(honkai_impact)",
    "source": "honkai_starrail",
    "source_cn": "崩坏：星穹铁道",
    "icon_url": "..."
  }
]
```

## 开发者信息

- **版本**: 1.0.0
- **作者**: kotone
- **许可**: MIT

## 更新日志

### v1.0.0 (2025-12-17)
- ✨ 初始版本发布
- 📚 支持四个游戏的角色数据
- 🎨 两种输出格式
- 🔄 自动数据加载和缓存

## 常见问题

### Q: 节点显示"无角色数据"？
A: 检查 `output` 目录是否存在对应的 JSON 文件，文件名是否正确。

### Q: 如何添加新的游戏/作品？
A: 
1. 在 `output/` 目录中添加新的 JSON 文件
2. 修改 `character_tag_selector.py` 中的 `DATA_FILES` 字典

### Q: 如何更新角色数据？
A: 直接更新 `output/` 目录中的 JSON 文件，然后重启 ComfyUI。

